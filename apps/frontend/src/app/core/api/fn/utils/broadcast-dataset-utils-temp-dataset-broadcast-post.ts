/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http'
import { Observable } from 'rxjs'
import { filter, map } from 'rxjs/operators'
import { StrictHttpResponse } from '../../strict-http-response'
import { requestBuilders } from '../../request-builders'

import { BroadcastResponse } from '../../models/broadcast-response'
import { DatasetBroadcastRequest } from '../../models/dataset-broadcast-request'

export interface BroadcastDatasetUtilsTempDatasetBroadcastPost$Params {
  body: DatasetBroadcastRequest
}

export function broadcastDatasetUtilsTempDatasetBroadcastPost(
  http: HttpClient,
  rootUrl: string,
  params: BroadcastDatasetUtilsTempDatasetBroadcastPost$Params,
  context?: HttpContext
): Observable<StrictHttpResponse<BroadcastResponse>> {
  const rb = new requestBuilders(
    rootUrl,
    broadcastDatasetUtilsTempDatasetBroadcastPost.PATH,
    'post'
  )
  if (params) {
    rb.body(params.body, 'application/json')
  }

  return http
    .request(
      rb.build({ responseType: 'json', accept: 'application/json', context })
    )
    .pipe(
      filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
      map((r: HttpResponse<any>) => {
        return r as StrictHttpResponse<BroadcastResponse>
      })
    )
}

broadcastDatasetUtilsTempDatasetBroadcastPost.PATH =
  '/utils/temp/dataset/broadcast'
