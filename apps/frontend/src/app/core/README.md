# Core Module

This folder contains singleton services, authentication logic, HTTP interceptors, guards, and other core application functionality that should be imported only once at the application root level.

## Contents

- **auth/** - Authentication services, guards, and models
- **layout/** - Application shell components (header, footer, navigation)
- Core services that are used across the entire application
- HTTP interceptors
- Application-wide configurations and constants
